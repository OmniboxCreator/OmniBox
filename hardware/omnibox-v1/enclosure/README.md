# OmniBox Board 1.0 Beta Enclosure

The recommended V1 enclosure is the sheet-metal design in `sheetmetal/`.

The `print3d/` directory contains auxiliary printable parts. Generated STEP/STL
files are included so the enclosure can be reviewed or quoted without running
the CAD helper scripts.
